services:
  courtbot:
    build: .
    container_name: courtbotadmin
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./config.json:/app/config.json:ro
    environment:
      - PYTHONUNBUFFERED=1
    stdin_open: true
    tty: true